name: CI
on: 
  push:
    paths: 
      - '**.sbt'
      - 'src/**'
      - 'project/**'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: olafurpg/setup-scala@v13
        with:
          cache: sbt
      - run: sbt 'formatCheckAll'
