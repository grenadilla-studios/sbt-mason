name: CI
on: 
  push:
  pull_request:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: olafurpg/setup-scala@v13
      - run: sbt 'scalafixAll --check; scalafmtSbtCheck; scalafmtCheckAll'
